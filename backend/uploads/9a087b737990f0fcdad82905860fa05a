from machine import Pin, I2C
from ssd1306 import SSD1306_I2C

def main():

    i2c_dev = I2C(1, scl=Pin(27), sda=Pin(26), freq=200000)
    oled = SSD1306_I2C(128, 64, i2c_dev)
    oled.text("Hola Mundo", 5, 5)
    oled.show()

if __name__ == '__main__':
    main()
